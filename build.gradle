buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.0.1'
        // 已移除 Realm 插件依赖
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
